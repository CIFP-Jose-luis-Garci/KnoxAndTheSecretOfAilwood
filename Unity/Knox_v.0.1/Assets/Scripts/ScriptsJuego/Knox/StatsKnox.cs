using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StatsKnox : MonoBehaviour
{

    public int lifes;

    // Start is called before the first frame update
    void Start()
    {
        lifes = 6;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
